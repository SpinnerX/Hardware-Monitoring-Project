// These are some commands that wil be utilized 
// Command for merging branchs
git merge master

// Change between branches
git branch <branch_name>
Example: git branch featrure_1

// showing logs of commits
git log

// To push the branches instead of making them locally and push them to github
// is by doing git push origin <branch_name>
